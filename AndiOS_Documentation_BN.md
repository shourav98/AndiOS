---
title: "AndiOS - AI রিয়েল এস্টেট অ্যাসিস্ট্যান্ট"
author: "Antigravity (AI Assistant)"
date: "August 2026"
---

# AndiOS: প্রজেক্ট গাইড ও আর্কিটেকচার (বইয়ের মতো বিস্তারিত)

AndiOS হলো দুবাইয়ের রিয়েল এস্টেট এজেন্সিগুলোর জন্য তৈরি করা একটি অত্যাধুনিক, AI-চালিত লিড ম্যানেজমেন্ট এবং CRM (Customer Relationship Management) সিস্টেম। এই বই বা গাইডটিতে আমরা AndiOS এর পেছনের প্রযুক্তি, ডাটাবেস, এআই (AI) এর ভূমিকা এবং পুরো সিস্টেমটি কীভাবে কাজ করে তার খুঁটিনাটি খুব সহজ ভাষায় আলোচনা করব।

---

## ১. AndiOS আসলে কী?
দুবাইয়ের রিয়েল এস্টেট মার্কেটে প্রতিদিন হাজার হাজার লিড (সম্ভাব্য ক্রেতা বা ভাড়াটিয়া) তৈরি হয়। কিন্তু এত লিডকে ম্যানুয়ালি ট্র্যাক করা, মেসেজ দেওয়া, এবং তাদের রিকোয়ারমেন্ট বোঝা মানুষের জন্য অনেক সময়সাপেক্ষ।

**AndiOS** এই সমস্যাটি সমাধান করে। এটি এমন একটি সিস্টেম যা:
- বিভিন্ন সোর্স (যেমন: Property Finder, WhatsApp) থেকে আসা লিডগুলোকে অটোমেটিক ডাটাবেসে সেভ করে।
- **Andi** নামের একটি AI (Artificial Intelligence) এজেন্টের মাধ্যমে কাস্টমারের সাথে সরাসরি WhatsApp-এ চ্যাট করে তাদের রিকোয়ারমেন্ট (বাজেট, বেডরুম, লোকেশন) জেনে নেয়।
- কাস্টমার রেডি হলে তাকে সরাসরি একজন হিউম্যান (মানুষ) এজেন্টের কাছে ট্রান্সফার করে দেয় (যাকে আমরা **Handover** বলি)।
- প্রপার্টি ভিজিটের জন্য গুগল ক্যালেন্ডারে টাইম স্লট বুকিং করে দেয়।

---

## ২. সিস্টেমের কোর টেকনোলজি (Technology Stack)
AndiOS কোনো সাধারণ সফটওয়্যার নয়, এটি তৈরি করা হয়েছে আধুনিক ও ফাস্ট কিছু টেকনোলজি দিয়ে:

- **FastAPI (Python):** সিস্টেমের মূল ব্যাকএন্ড বা ব্রেইন এটি দিয়ে তৈরি। এটি খুব দ্রুত কাজ করে এবং রিয়েল-টাইম ডেটা হ্যান্ডেল করতে পারে।
- **Supabase:** এটি হলো ডাটাবেস। কাস্টমারদের তথ্য, চ্যাটের হিস্ট্রি, এজেন্টের লিস্ট—সবকিছু এখানে সেভ থাকে।
- **OpenAI GPT-4o:** AI চ্যাটবট (Andi) এর ব্রেইন হলো এটি। এটি কাস্টমারের কথা বুঝতে পারে এবং একদম মানুষের মতো উত্তর দিতে পারে।
- **WhatsApp Webhooks (360dialog/Twilio):** কাস্টমারদের WhatsApp-এ মেসেজ পাঠানো এবং রিসিভ করার জন্য এটি ব্যবহার করা হয়।
- **Google Calendar API:** কাস্টমাররা কখন প্রপার্টি দেখতে আসবে, সেই টাইম স্লট ম্যানেজ করার জন্য এটি যুক্ত করা হয়েছে।

---

## ৩. লিড (Lead) এবং এর লাইফসাইকেল
রিয়েল এস্টেটের ভাষায় "লিড" হলো একজন সম্ভাব্য কাস্টমার। AndiOS সিস্টেমে একটি লিড কীভাবে ম্যানেজ হয়, তার ধাপগুলো নিচে দেওয়া হলো:

1. **ক্যাপচার (Capture):** যখনই কেউ Property Finder-এ বিজ্ঞাপন দেখে ফর্ম পূরণ করে, তখন Webhook এর মাধ্যমে সেই ডেটা আমাদের সিস্টেমে চলে আসে।
2. **অ্যাসাইন (Assign):** সিস্টেম লিডটিকে একটি এজেন্সির নির্দিষ্ট এজেন্টের আন্ডারে দিয়ে দেয়।
3. **AI গ্রিটিং (AI Greeting):** লিড ডাটাবেসে আসার সাথে সাথে AI এজেন্ট (Andi) কাস্টমারের WhatsApp-এ প্রথম মেসেজটি পাঠায়। 
4. **কোয়ালিফিকেশন (Qualification):** AI কাস্টমারের সাথে কথা বলে জানার চেষ্টা করে তার বাজেট কত, সে কী কিনতে চায় নাকি ভাড়া নিতে চায়, এবং কবে মুভ করতে চায়।
5. **হ্যান্ডওভার (Handover):** কাস্টমার যখন প্রপার্টি দেখতে চায় বা মানুষের সাথে কথা বলতে চায়, তখন AI কন্ট্রোল ছেড়ে দেয় এবং হিউম্যান এজেন্ট চ্যাট শুরু করে।

---

## ৪. AI কোয়ালিফিকেশন এবং লিড স্কোরিং (Lead Scoring)
AndiOS এর একটি অসাধারণ ফিচার হলো **AI Lead Qualification**।

AI (Andi) যখন কাস্টমারের সাথে কথা বলে, তখন সে ব্যাকগ্রাউন্ডে চুপচাপ কয়েকটি জিনিস নোট করতে থাকে:
- কাস্টমারের বাজেট (Budget Min/Max)
- বেডরুমের সংখ্যা (Bedrooms)
- লোকেশন পছন্দ (Location Preference)
- মুভ-ইন টাইমলাইন (Move-in Timeline)

এই ডেটাগুলো পাওয়ার পর সিস্টেম **score_lead** নামের একটি ফাংশন ব্যবহার করে কাস্টমারকে 0 থেকে 100 এর মধ্যে একটি "স্কোর" দেয়। 
- স্কোর বেশি হলে বোঝা যায় কাস্টমারটি খুবই সিরিয়াস (High Quality Lead)।
- স্কোর কম হলে বোঝা যায় কাস্টমারটি হয়তো শুধু মার্কেট যাচাই করছে।

---

## ৫. হ্যান্ডওভার মেকানিজম (AI থেকে মানুষের কাছে কন্ট্রোল)
ডাটাবেসের `leads` টেবিলে `is_ai_handling` নামে একটি ফ্ল্যাগ (True/False) থাকে।

### ক. অটোমেটিক হ্যান্ডওভার:
AI সব সময় কাস্টমারের কথাগুলো বিশ্লেষণ করে (detect_handover ফাংশনের মাধ্যমে)। যদি কাস্টমার কোনো আইনি প্রশ্ন করে, দাম নিয়ে তর্ক করে, খারাপ ভাষা ব্যবহার করে বা সরাসরি মানুষের সাথে কথা বলতে চায়—তখন AI নিজে থেকেই `is_ai_handling = False` করে দেয়।

### খ. ম্যানুয়াল হ্যান্ডওভার:
হিউম্যান এজেন্ট ড্যাশবোর্ড থেকে চাইলে যেকোনো সময় "Take Over" বাটনে ক্লিক করে AI কে থামিয়ে দিয়ে নিজে চ্যাট শুরু করতে পারেন।

হ্যান্ডওভার হওয়ার পর AI আর কোনো মেসেজ দেয় না।

---

## ৬. প্রপার্টি ভিউইং এবং ক্যালেন্ডার (Viewing & Calendar)
কাস্টমার যখন বলে "আমি আগামীকাল প্রপার্টি দেখতে চাই", তখন AI কী করে?

1. AI ব্যাকগ্রাউন্ডে এজেন্টের **Google Calendar** চেক করে দেখে কোন কোন স্লট ফাঁকা আছে (যেমন: বিকেল ৩টা বা ৫টা)।
2. AI কাস্টমারকে অপশন দেয়: "আগামীকাল বিকেল ৩টা নাকি ৫টায় আসবেন?"
3. কাস্টমার সময় কনফার্ম করলে, সিস্টেম অটোমেটিক ডাটাবেসে একটি "Viewing" ক্রিয়েট করে এবং এজেন্টের গুগল ক্যালেন্ডারে একটি ইভেন্ট (Event) বানিয়ে দেয়।
4. এরপর সিস্টেম থেকে অটোমেটিক রিমাইন্ডার মেসেজ যেতে থাকে (২৪ ঘণ্টা আগে, ২ ঘণ্টা আগে)।

---

## ৭. ডাটাবেস ও মাল্টি-টেনেন্সি (Multi-tenancy)
AndiOS কোনো একটি নির্দিষ্ট এজেন্সির জন্য নয়। এটি একটি **SaaS (Software as a Service)** প্ল্যাটফর্ম। অর্থাৎ, একাধিক রিয়েল এস্টেট কোম্পানি (যেমন: Agency A, Agency B) একই সফটওয়্যার ব্যবহার করতে পারবে, কিন্তু কেউ কারও ডেটা দেখতে পারবে না।

এটির জন্য ডাটাবেসে **Row Level Security (RLS)** এবং **Multi-tenancy** আর্কিটেকচার ব্যবহার করা হয়েছে। প্রতিটি টেবিলে `agency_id` থাকে। একজন এজেন্ট যখন সিস্টেমে লগইন করেন, তিনি শুধু তার নিজের এজেন্সির ডেটাই দেখতে পান।

---

## ৮. ওনার রিপোর্ট (Owner Reports)
রিয়েল এস্টেট কোম্পানিগুলোকে প্রায়ই প্রপার্টির মালিকদের (Owners) রিপোর্ট দিতে হয় যে তাদের প্রপার্টি নিয়ে কেমন কাজ হচ্ছে। AndiOS এই কাজটিও AI দিয়ে করে ফেলে!

**AI Report Generation:**
সিস্টেম গত এক সপ্তাহের কতগুলো লিড এসেছে, কতগুলো ভিউইং হয়েছে, কতজন কাস্টমার আগ্রহী—এই সব ডেটা একসাথে করে GPT-4o কে পাঠায়। GPT-4o তখন চমৎকার প্রফেশনাল ভাষায় একটি "Executive Summary" বা রিপোর্ট লিখে দেয়, যা সরাসরি মালিককে পাঠানো যায়।

---

## ৯. ভবিষ্যৎ পরিকল্পনা (Phase 2 & Phase 3)
AndiOS কে আরও শক্তিশালী করার জন্য ভবিষ্যতে যে ফিচারগুলো যুক্ত হবে:
- **Phase 2 (ডকুমেন্টেশন):** ভাড়ার কন্ট্রাক্ট (Tenancy Contracts), রসিদ (Cheques), এবং ইনভয়েস অটোমেটিক তৈরি করার সিস্টেম।
- **Phase 3 (Sami Voice Bot):** শুধু চ্যাট নয়, "Sami" নামের একটি AI ভয়েস বট কাস্টমারদের সরাসরি ফোন কল করে কথা বলতে পারবে।

---

## উপসংহার
AndiOS হলো রিয়েল এস্টেট ব্রোকারেজ বিজনেসকে ডিজিটাল এবং অটোমেট করার একটি সম্পূর্ণ প্যাকেজ। এটি শুধুমাত্র সময় বাঁচায় না, বরং কাস্টমারদের একটি প্রফেশনাল এবং দ্রুত অভিজ্ঞতা (Fast Experience) দেয়। AI-কে মানুষের পরিপূরক হিসেবে ব্যবহার করে সেলস এবং কনভার্শন রেট বাড়ানোর এক অসাধারণ সিস্টেম হলো **AndiOS**।
